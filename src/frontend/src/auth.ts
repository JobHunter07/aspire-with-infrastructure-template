// auth removed from frontend. Server (BFF) handles all auth interactions with Keycloak.
// This file is intentionally left blank to avoid bundling server-side auth libraries into the frontend.

export {};
