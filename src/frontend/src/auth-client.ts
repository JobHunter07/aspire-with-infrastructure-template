// auth-client removed. Frontend is BFF-only and should have zero auth libraries.
// The server handles authentication via /auth endpoints.
// This file intentionally left blank to avoid importing better-auth in the frontend bundle.

export {};
